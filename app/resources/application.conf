ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [vn.com.libertime.ApplicationKt.module]
  }
}

key {
  secret = ${HASH_SECRET_KEY}
}

redis {
  production {
    secret = ${REDIS_SECRET_KEY}
    host = "redis"
    port = 6379
  }
  sandbox {
    secret = ${REDIS_SECRET_KEY}
    host = "localhost"
    port = 6379
  }
}