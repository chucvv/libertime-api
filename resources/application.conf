ktor {
  deployment {
    sandbox {
      host = "localhost"
      port = 8080
    }
    staging {
      host = "staging.liber.vn.com"
      port = 8080
    }
    production {
      host = "liber.vn.com"
      port = 8080
    }
  }
  application {
    modules = [vn.com.libertime]
  }
}